<h1><%= current_user.name %></h1>
<span><%= link_to t("view_my_profile"), current_user %></span>
<span><%= t(".microposts.count_micropost", count: current_user.microposts.count) %>,
 <%= t("micoposts.text") %></span>
